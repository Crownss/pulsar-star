[package]
name = "pulsar-star"
version = "0.1.0"
edition = "2024"

[profile.release]
lto = "thin"

[dependencies]
pnet = { version = "0.35.0", features = ["pcap"] }
libc = "0.2.189"
clap = { version = "4.6.6", features = ["derive"] }
clap_derive = { version = "4.0.0-rc.1" }
